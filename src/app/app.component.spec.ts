import { describe, it, expect, beforeEach } from 'vitest';
describe('Prueba para SonarQube', () => {
  it('debería pasar siempre en verde para generar el lcov.info', () => {
    
    expect(true).toBe(true);
  });
});